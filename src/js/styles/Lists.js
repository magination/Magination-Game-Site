var imgUrls = require('../config/config').urls.img;

var ListStyles = {
	Magination: {
		listStyleImage: 'url(' + imgUrls.listMarkerBlueSingle + ')',
		listStylePosition: 'inside',
		padding: '0'
	}
};

module.exports = ListStyles;
